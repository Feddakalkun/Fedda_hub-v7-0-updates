import WorkflowChainBuilder from '@/components/WorkflowChainBuilder';

export const metadata = {
    title: 'Workflow Chain Builder | FanVue Hub',
    description: 'Chain together Image → Lipsync → Wan2.1 workflows'
};

export default function WorkflowChainPage() {
    return <WorkflowChainBuilder />;
}
